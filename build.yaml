board: [ nice_nano//zmk ]

include:

# ── Reset Firmware ────────────────────────────────────────────────────────────
  - name: settings_reset
    board: nice_nano//zmk
    shield: settings_reset

# ── Bluetooth (BT) Firmwares ─────────────────────────────────────────────────────────────
  - name: bt
    board: nice_nano//zmk
    shield:
      - charybdis_left_bt
      - charybdis_right_bt
    keymap:
      - qwerty
      - colemak_dh
      # - canary
      # - focal
      # - graphite
